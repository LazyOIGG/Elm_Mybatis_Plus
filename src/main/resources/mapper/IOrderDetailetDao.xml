<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ynu.edu.dao.IOrderDetailetDao">

    <!-- 批量保存订单明细 -->
    <insert id="saveOrderDetailetBatch" parameterType="java.util.List">
        INSERT INTO orderDetailet(orderId, foodId, quantity) VALUES
        <foreach collection="list" item="od" separator=",">
            (#{od.orderId}, #{od.foodId}, #{od.quantity})
        </foreach>
    </insert>

    <!-- 根据订单ID查询订单明细 -->
    <select id="listOrderDetailetByOrderId" parameterType="Integer" resultType="ynu.edu.entity.OrderDetailet">
        SELECT * FROM orderDetailet WHERE orderId = #{orderId}
    </select>

    <!-- 根据订单ID查询订单明细（简单的查询） -->
    <select id="listOrderDetailetByOrderIdSimple" parameterType="Integer" resultType="ynu.edu.entity.OrderDetailet">
        SELECT * FROM orderDetailet WHERE orderId = #{orderId}
    </select>

    <!-- 根据订单ID和食品ID查询明细 -->
    <select id="getOrderDetailetByIds" resultType="ynu.edu.entity.OrderDetailet">
        SELECT * FROM orderDetailet
        WHERE orderId = #{orderId} AND foodId = #{foodId}
    </select>
</mapper>